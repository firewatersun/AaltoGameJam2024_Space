using UnityEngine;

public class GravityValues : MonoBehaviour
{
    public static float gravValue = 2.1f;
    public static float gravRadiusMultiplier = 20f;

    public static float minGravForce = 0.6f;
    public static float maxGravForce = 2.1f;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
